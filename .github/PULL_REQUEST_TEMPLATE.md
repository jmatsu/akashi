<!--
Open an issue and let it be discussed before writing a pull request:
see CONTRIBUTING.md. An unannounced pull request may be closed with nothing
said against the code in it.
-->

Closes #

## What this changes

## Why

## Checked

- [ ] `npm run format:check` and `npm run lint`
- [ ] `npm run build`, then `npm test`
- [ ] Tried in a browser — say which, and on what OS
- [ ] Nothing here reaches the network, adds a dependency, or needs `src/csp.ts`,
      `biome.json` or `test/offline.test.ts` relaxed
